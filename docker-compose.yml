services:
  postgres:
    image: postgres:16

    container_name: matchscoreai-postgres

    environment:
      POSTGRES_DB: matchscoreai
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres

    ports:
      - "5432:5432"